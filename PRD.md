# PRD — إندومكس 🍜 (IndoMix)

## 1. نظرة عامة

### 1.1 ملخص المنتج
إندومكس هو براند متخصص في الإندومي ووصفاته المبتكرة. يشمل المنتج **موقعاً إلكترونياً كاملاً** يخدم شريحتين:

1. **واجهة العملاء**: تصفح قائمة الأصناف، تفاصيل المنتج، السلة، المفضلة، العروض، معلومات المطعم، وبوت أسئلة شائعة.
2. **لوحة تحكم للأدمن**: إدارة كاملة لمحتوى الموقع (منتجات، تصنيفات، عروض، مراجعات، بوت FAQ، إعدادات المطعم).

### 1.2 المشكلة
يحتاج المطعم إلى قناة رقمية موحدة لعرض أصنافه وعروضه، واستقبال الطلبات عبر الهاتف/واتساب، وإدارة محتوى الموقع دون تدخّل مطوّر.

### 1.3 الحل
موقع SSR سريع ومتجاوب مع لوحة تحكم مركزية مبنية على Supabase، حيث يتم الطلب النهائي عبر الهاتف أو واتساب (بدون بوابة دفع إلكتروني في المرحلة الحالية).

---

## 2. الأهداف والنجاح

| الهدف | مؤشر القياس |
|-------|-------------|
| تسهيل تصفح القائمة والطلب | زمن الوصول للسلة < 3 نقرات من الصفحة الرئيسية |
| رفع معدل الطلبات عبر واتساب | عدد نقرات "اطلب عبر واتساب" أسبوعياً |
| استقلالية الأدمن بإدارة المحتوى | تنفيذ كل عمليات CRUD من اللوحة بدون كود |
| أداء وتجربة استخدام عالية | Lighthouse Performance ≥ 90 على الجوال |

### 2.1 غير الأهداف (Out of Scope)
- الدفع الإلكتروني / بوابة دفع.
- حسابات عملاء (تسجيل/تسجيل دخول للزوار).
- تطبيق جوال أصلي.
- تتبع حالة الطلب لحظياً.

---

## 3. الجمهور المستهدف والشخصيات

### 3.1 العميل (Persona: الزائر الجائع)
- عمر 16–35، يتصفح من الجوال غالباً، يبحث عن وجبة سريعة بسعر مناسب.
- يحتاج: قائمة واضحة بالصور والأسعار، عروض بارزة، طريقة طلب فورية (واتساب/هاتف).
- يدعم العربية (RTL) بشكل كامل.

### 3.2 صاحب المطعم / الأدمن (Persona: المدير)
- غير تقني، يحتاج لوحة تحكم بسيطة لتحديث الأسعار والأصناف والعروض يومياً.
- يسجل دخوله بحساب واحد (email + password) عبر Supabase Auth.

---

## 4. المتطلبات الوظيفية

### 4.1 واجهة العملاء

#### FR-1: الصفحة الرئيسية (`/`)
- Hero section بهوية البراند مع أنيميشن (GSAP + Lenis smooth scroll).
- عرض التصنيفات النشطة مع صورها ونص الشارة (badge_text).
- قسم "الأكثر طلباً" (المنتجات ذات `is_popular`).
- قسم العروض النشطة.
- معرض مراجعات العملاء (reviews_gallery) كسلايدر (Embla Carousel).
- روابط سريعة: القائمة، العروض، تواصل معنا.

#### FR-2: القائمة (`/menu`)
- عرض كل المنتجات المتاحة (`is_available = true`) مقسمة حسب التصنيفات النشطة بترتيب `display_order`.
- بحث/فلترة داخل القائمة حسب التصنيف.
- بطاقة المنتج تعرض: الصورة، الاسم، الوصف المختصر، السعر، السعر القديم إن وجد، التقييم، عدد المراجعات، الوسم (tag)، وزر إضافة للسلة وزر المفضلة.
- المنتجات غير المتاحة تظهر بحالة معطّلة (غير قابلة للإضافة).

#### FR-3: تفاصيل المنتج (`/products/:id`)
- عرض كامل التفاصيل: الوصف الكامل، السعر، الصورة، التقييم.
- عرض الإضافات (extras) المتاحة لكل منتج مع سعرها، وإمكانية اختيارها لتُضاف للسلة.
- منتجات مشابهة من نفس التصنيف.

#### FR-4: السلة (`/cart`)
- إضافة/تعديل كمية/حذف أصناف مع دعم الإضافات المختارة.
- حساب المجموع الفرعي + رسوم التوصيل الافتراضية (20 ج.هـ، قابلة للتحديث من إعدادات الأدمن).
- حفظ السلة في `localStorage` (`indomix_cart_v1`) لتستمر بين الزيارات.
- زر "إتمام الطلب" ينقل تفاصيل السلة إلى واتساب برسالة جاهزة، أو الاتصال الهاتفي مباشرة.

#### FR-5: المفضلة (`/favorites`)
- إضافة/إزالة منتجات من المفضلة (محفوظة في `localStorage` باسم `indomix_favorites`).
- عرض المنتجات المفضلة مع إمكانية نقلها للسلة.

#### FR-6: العروض (`/offers`)
- عرض العروض النشطة فقط (`is_active = true`) مع: العنوان، الوسم، شارة الخصم، الوصف، محتويات العرض (items[])، السعر مقابل السعر القديم، تاريخ الصلاحية.
- ربط العرض بمنتج مرتبط (`associated_product_id`) لإضافة العرض للسلة مباشرة.

#### FR-7: من نحن (`/about`) و تواصل معنا (`/contact`)
- صفحة تعريفية بالبراند وقصته.
- بيانات التواصل تُقرأ من إعدادات المطعم (Supabase) مع قيم احتياطية ثابتة:
  - هاتف: `01015770734`
  - واتساب: `201015770734`
  - مواعيد العمل: "يوميًا من ١١:٠٠ صباحًا حتى ٣:٠٠ فجرًا"
- نموذج/أزرار تواصل مباشر (call / WhatsApp).

#### FR-8: بوت الأسئلة الشائعة (FaqBotWidget)
- ودجت عائم في كل صفحات الموقع.
- يجيب تلقائياً عن طريق مطابقة كلمات مفتاحية (`keywords[]`) من جدول `bot_faq`.
- عرض الأسئلة المقترحة النشطة بترتيب `display_order`، والرد على الأسئلة الحرجة برسالة تحويل المستخدم للتواصل المباشر.

### 4.2 لوحة التحكم (`/admin`)

#### FR-9: المصادقة
- تسجيل دخول واحد للأدمن عبر **Supabase Auth** (email + password).
- جدول `profiles` يربط مستخدم Auth بدور `admin` (قيمة وحيدة مسموحة).
- حماية جميع عمليات الكتابة عبر RLS بدالة `is_admin()`.
- حماية مسار `/admin` — إعادة توجيه غير المسجلين لصفحة الدخول.
- خروج (logout) وإدارة الجلسة.

#### FR-10: إدارة المنتجات (ProductsTab)
- CRUD كامل: إضافة/تعديل/حذف منتج.
- الحقول: id، التصنيف، الاسم، الوصف الكامل والمختصر، السعر (+ سعر قديم اختياري)، الصورة (رفع/رابط)، الوسم، التقييم وعدد المراجعات، شارات "الأكثر طلباً" و"متاح".
- إدارة إضافات المنتج (product_extras): اسم + سعر + توفر.
- التحقق من صحة المدخلات (Zod) — السعر ≥ 0.

#### FR-11: إدارة التصنيفات (CategoriesTab)
- CRUD كامل للتصنيفات: id، الاسم، ترتيب العرض، الصورة، نص الشارة، نشط/معطل.

#### FR-12: إدارة العروض (OffersTab)
- CRUD كامل للعروض بكل حقولها (العنوان، الوسم، شارة الخصم، الوصف، items[]، السعر، السعر القديم، الصورة، المنتج المرتبط، تاريخ الانتهاء، نشط).

#### FR-13: إدارة المراجعات والمعرض (ReviewsGalleryTab)
- CRUD لصور مراجعات العملاء: صورة + عنوان + ترتيب عرض + نشط.

#### FR-14: إدارة بوت FAQ (FaqTab)
- CRUD للأسئلة: السؤال، الكلمات المفتاحية، الرد، ترتيب العرض، نشط.

#### FR-15: إعدادات المطعم (SettingsTab)
- إعدادات key/value (jsonb): رقم الهاتف، واتساب، مواعيد العمل، رسوم التوصيل، وأي إعدادات مستقبلية.
- التعديلات تنعكس فوراً على واجهة العملاء.

---

## 5. المتطلبات غير الوظيفية

| # | المتطلب |
|---|---------|
| NFR-1 | **الأداء**: SSR عبر TanStack Start + Vite 8؛ تحميل سريع على شبكات 3G؛ صور WebP. |
| NFR-2 | **التجاوب**: تصميم Mobile-first يعمل على الجوال والتابلت وسطح المكتب. |
| NFR-3 | **اللغة والاتجاه**: عربي كامل RTL، خط Cairo. |
| NFR-4 | **الأمان**: Row Level Security على كل الجداول؛ القراءة عامة للبيانات النشطة، الكتابة للأدمن فقط؛ anon key آمن للاستخدام في المتصفح. |
| NFR-5 | **الحماية من الأخطاء**: معالجة أخطاء موحدة في طبقة lib/error handling + تنبيهات Sonner toast. |
| NFR-6 | **إمكانية الوصول**: تباين ألوان مناسب، تنقل بلوحة المفاتيح، نصوص بديلة للصور. |
| NFR-7 | **SEO**: SSR + meta tags لكل صفحة. |
| NFR-8 | **الاستمرارية**: السلة والمفضلة تُحفظ محلياً وتُستعاد عند العودة. |

---

## 6. البنية التقنية

### 6.1 Stack

| الطبقة | التقنية |
|--------|---------|
| Frontend | React 19 + TypeScript |
| Routing/SSR | TanStack Router + TanStack Start (file-based routes) |
| Styling | Tailwind CSS v4 + tw-animate-css |
| UI Components | shadcn/ui (components/ui) |
| State/Data | TanStack Query + React Context (AuthContext, StoreContext) |
| Database/Auth | Supabase (PostgreSQL 17 + RLS + Auth) |
| Animations | GSAP + Lenis + Embla Carousel |
| Validation | Zod |
| Build | Vite 8 (Nitro) — Package Manager: bun |
| Deployment | Vercel (vercel.json) — بديل: Cloudflare Workers (`NITRO_PRESET=cloudflare`) |

### 6.2 مخطط قاعدة البيانات

| الجدول | الغرض | أهم الحقول |
|--------|-------|-----------|
| `products` | الأصناف | id(text), category_id, name, description, short_description, price, old_price, image_url, tag, rating, reviews_count, is_popular, is_available |
| `categories` | التصنيفات | id(text), name, display_order, image_url, is_active, badge_text |
| `product_extras` | إضافات المنتج | product_id, name, price, is_available |
| `offers` | العروض | title, tag, discount_badge, description, items[], price, old_price, image_url, associated_product_id, valid_until, is_active |
| `reviews_gallery` | معرض المراجعات | image_url, title, display_order, is_active |
| `bot_faq` | أسئلة البوت | question, keywords[], answer, display_order, is_active |
| `restaurant_settings` | إعدادات المطعم | key, value(jsonb) |
| `profiles` | أدوار المستخدمين | id (=auth.uid), role ('admin' فقط) |

- دالة `is_admin()` SECURITY DEFINER تُستخدم في سياسات RLS.
- Migrations موجودة في `supabase/migrations/` + نسخة مجمعة `schema.sql` وبيانات أولية `seed.sql`.

### 6.3 هيكل المصدر

```
src/
├── assets/           # صور ثابتة (jpg + webp)
├── components/
│   ├── admin/        # لوحة التحكم (tabs/, modals)
│   ├── site/         # واجهة العملاء (Header, Footer, Cards, FaqBotWidget/)
│   └── ui/           # shadcn/ui primitives
├── constants/        # ثوابت المطعم + مفاتيح localStorage
├── context/          # AuthContext, StoreContext (Cart/Favorites)
├── hooks/            # TanStack Query hooks (hooks/admin/)
├── lib/              # Supabase client, utils, error handling
├── routes/           # index, menu, products/$id, cart, favorites,
│                     # offers, about, contact, admin
└── types/            # database.ts, product.ts, review.ts, store.ts
```

### 6.4 متغيرات البيئة

| المتغير | الوصف |
|---------|-------|
| `VITE_SUPABASE_URL` | رابط مشروع Supabase |
| `VITE_SUPABASE_ANON_KEY` | المفتاح العام (anon) — آمن للمتصفح، الحماية عبر RLS |

---

## 7. تجربة المستخدم (UX)

### 7.1 رحلة العميل الأساسية
1. يدخل للصفحة الرئيسية ← يتأثر بالعروض/Hero.
2. يتصفح القائمة أو يفتح منتجاً شائعاً مباشرة.
3. يختار الإضافات ويضيف للسلة (أو يحفظ بالمفضلة).
4. يفتح السلة ← يراجع الإجمالي مع التوصيل.
5. يضغط "إتمام الطلب" ← تُرسل تفاصيل السلة كرسالة واتساب جاهزة.

### 7.2 رحلة الأدمن الأساسية
1. `/admin` ← تسجيل دخول.
2. يختار التبويب المطلوب (منتجات/تصنيفات/عروض/مراجعات/FAQ/إعدادات).
3. ينفذ التعديل عبر Modal مع تحقق من المدخلات ← حفظ فوري في Supabase.

### 7.3 الحالات الحدّية
- سلة فارغة / مفضلة فارغة: Empty state مع CTA للقائمة.
- منتج غير متاح: معطّل بصرياً ولا يضاف للسلة.
- انقطاع الاتصال/خطأ Supabase: رسالة خطأ واضحة + إعادة المحاولة.
- عرض منتهي الصلاحية: لا يظهر للعملاء.

---

## 8. مؤشرات القياس (KPIs)

- عدد الطلبات عبر واتساب (نقرات زر الإتمام) أسبوعياً.
- عدد زيارات صفحة القائمة وصفحة تفاصيل المنتج.
- معدل إضافة للسلة (Add-to-cart rate).
- متوسط قيمة السلة.
- وقت تشغيل الموقع (Uptime ≥ 99.5%).

---

## 9. المخاطر والتبعيات

| المخاطرة | الأثر | التخفيف |
|----------|------|---------|
| اعتماد الطلب على واتساب خارجياً | فقدان تتبع الطلبات | مستقبلاً: نظام طلبات داخلي |
| مفتاح anon مكشوف في المتصفح | إساءة استخدام كتابة | RLS صارم + اختبار السياسات دورياً |
| حساب أدمن واحد | حجب إذا فُقدت بياناته | توثيق الاستعادة عبر Supabase Dashboard |
| صور كبيرة تبطئ الموقع | تجربة سيئة على الجوال | ضغط WebP + lazy loading |

---

## 10. خارطة الطريق

### Phase 1 — الإطلاق (منجز حالياً)
- ✅ واجهة العملاء كاملة (رئيسية، قائمة، منتج، سلة، مفضلة، عروض، عن الموقع، تواصل).
- ✅ بوت FAQ + معرض المراجعات.
- ✅ لوحة تحكم الأدمن بكل التبويبات.
- ✅ النشر على Vercel.

### Phase 2 — التحسينات
- ⬜ نظام طلبات داخلي مع تتبع الحالة.
- ⬜ حسابات عملاء وسجل طلبات.
- ⬜ تكامل بوابة دفع.
- ⬜ إشعارات الطلبات (WhatsApp Business API).

### Phase 3 — التوسع
- ⬜ تعدد الفروع.
- ⬜ برنامج ولاء/نقاط.
- ⬜ تطبيق جوال (PWA ثم Native).

---

## 11. المعايير والتوثيق

- تشغيل محلي: `bun install` ثم `cp .env.example .env` ثم `bun run dev` على المنفذ 3000.
- فحص الجودة: `bun run lint` (ESLint) و `bun run format` (Prettier).
- بناء الإنتاج: `bun run build` ومعاينة عبر `bun run preview`.
- الترخيص: المشروع مملوك للعميل — جميع الحقوق محفوظة.
